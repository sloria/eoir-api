name: Publish acis-browser

on:
  push:
    tags:
      - "acis-browser-v*"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Verify tag matches package version
        env:
          TAG: ${{ github.ref_name }}
        run: |
          tag_version="${TAG#acis-browser-v}"
          pkg_version="$(uv version --package acis-browser --short --frozen)"
          if [ "$tag_version" != "$pkg_version" ]; then
            echo "Tag version '$tag_version' does not match packages/acis-browser version '$pkg_version'" >&2
            exit 1
          fi

      - name: Build distributions
        run: uv build --package acis-browser

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
